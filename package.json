{
  "name": "quant-fx-system",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "pnpm --filter frontend dev",
    "backend-dev": "make backend-dev",
    "test": "make test"
  }
}
